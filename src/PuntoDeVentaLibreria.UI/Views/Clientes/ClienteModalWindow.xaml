<Window x:Class="PuntoDeVentaLibreria.UI.Views.Clientes.ClienteModalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Ficha de Cliente" 
        Height="500" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8FAFC"
        FontFamily="Segoe UI Variable, Segoe UI, sans-serif">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="FICHA DEL CLIENTE" FontSize="18" FontWeight="Bold" Foreground="#0F172A"/>
            <TextBlock Text="Datos de contacto y cuenta corriente / fiados" FontSize="12" Foreground="#64748B"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Nombre y Apellido / Razón Social:" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBox x:Name="TxtNombre" Text="{Binding NombreCompleto, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="8,6" Margin="0,0,0,10"/>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,6,0">
                        <TextBlock Text="DNI / CUIT:" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding DniOCuit, UpdateSourceTrigger=PropertyChanged}" FontSize="13" Padding="8,6"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="6,0,0,0">
                        <TextBlock Text="Teléfono / WhatsApp:" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding Telefono, UpdateSourceTrigger=PropertyChanged}" FontSize="13" Padding="8,6"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Colegio / Escuela / Institución:" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBox Text="{Binding ColegioOInstitucion, UpdateSourceTrigger=PropertyChanged}" FontSize="13" Padding="8,6" Margin="0,0,0,10"/>

                <TextBlock Text="Dirección / Domicilio:" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBox Text="{Binding Direccion, UpdateSourceTrigger=PropertyChanged}" FontSize="13" Padding="8,6" Margin="0,0,0,12"/>

                <Border Background="White" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="8" Padding="12">
                    <StackPanel>
                        <CheckBox Content="Permitir Compras a Cuenta Corriente (Fiado)" IsChecked="{Binding PermiteFiado}" FontWeight="Bold" FontSize="13" Margin="0,0,0,8"/>
                        <TextBlock Text="Límite Máximo de Crédito ($):" FontSize="12" Foreground="#475569" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding LimiteCredito, UpdateSourceTrigger=PropertyChanged, StringFormat='N2'}" FontSize="14" FontWeight="Bold" Padding="8,6"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <DockPanel Grid.Row="2" Margin="0,16,0,0">
            <Button Content="Cancelar" Click="BtnCancelar_Click" Padding="16,8" FontSize="13" Cursor="Hand"/>
            <Button Content="💾 Guardar Cliente" Click="BtnGuardar_Click" HorizontalAlignment="Right"
                    Background="#2563EB" Foreground="White" FontWeight="Bold" Padding="20,8" FontSize="13" Cursor="Hand"/>
        </DockPanel>
    </Grid>
</Window>
