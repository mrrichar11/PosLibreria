<UserControl x:Class="PuntoDeVentaLibreria.UI.Views.Caja.CajaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             FontFamily="Segoe UI Variable, Segoe UI, sans-serif">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabecera -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="CAJA &amp; TURNOS DE MOSTRADOR" FontSize="20" FontWeight="Bold" Foreground="#0F172A"/>
                <TextBlock Text="Control de flujos de efectivo, arqueos y movimientos de mostrador" FontSize="12" Foreground="#64748B"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="➕ Gasto Operativo" Command="{Binding RegistrarGastoCommand}"
                        Padding="14,8" FontWeight="SemiBold" Margin="0,0,8,0" Cursor="Hand"/>
                <Button Content="💼 Retiro de Dueño" Command="{Binding RegistrarRetiroCommand}"
                        Padding="14,8" FontWeight="SemiBold" Margin="0,0,8,0" Cursor="Hand"/>
                <Button Content="🔒 Cerrar Turno (Arqueo)" Command="{Binding CerrarCajaCommand}"
                        Background="#DC2626" Foreground="White" FontWeight="Bold" Padding="16,8" Cursor="Hand"/>
            </StackPanel>
        </Grid>

        <!-- Tarjetas de Métricas de Caja -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Total Efectivo en Caja (Gigante) -->
            <Border Grid.Column="0" Background="#0F172A" CornerRadius="10" Padding="16,12" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="EFECTIVO EN CAJÓN" Foreground="#94A3B8" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalEfectivoEnCaja, StringFormat='${0:N2}'}" Foreground="#38BDF8" FontSize="24" FontWeight="Bold" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Ventas en Efectivo -->
            <Border Grid.Column="1" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="16,12" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="VENTAS EFECTIVO" Foreground="#64748B" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalVentasEfectivo, StringFormat='${0:N2}'}" Foreground="#16A34A" FontSize="20" FontWeight="Bold" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Ventas Digitales (Tarjetas/QR) -->
            <Border Grid.Column="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="16,12" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="TARJETAS &amp; TRANSFERENCIAS" Foreground="#64748B" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalVentasDigitales, StringFormat='${0:N2}'}" Foreground="#2563EB" FontSize="20" FontWeight="Bold" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <!-- Gastos y Retiros -->
            <Border Grid.Column="3" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10" Padding="16,12">
                <StackPanel>
                    <TextBlock Text="GASTOS / EGRESOS" Foreground="#64748B" FontSize="11" FontWeight="Bold"/>
                    <TextBlock Text="{Binding TotalGastos, StringFormat='${0:N2}'}" Foreground="#EF4444" FontSize="20" FontWeight="Bold" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Grilla de Movimientos del Turno -->
        <Border Grid.Row="2" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10">
            <DataGrid ItemsSource="{Binding Movimientos}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#F1F5F9"
                      RowHeight="42"
                      FontSize="13"
                      Background="White"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Fecha / Hora" Binding="{Binding FechaCreacion, StringFormat='dd/MM/yyyy HH:mm'}" Width="140"/>
                    <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="130"/>
                    <DataGridTextColumn Header="Concepto" Binding="{Binding Concepto}" Width="*"/>
                    <DataGridTextColumn Header="Medio de Pago" Binding="{Binding MetodoPago}" Width="120"/>
                    <DataGridTextColumn Header="Usuario" Binding="{Binding UsuarioNombre}" Width="110"/>
                    <DataGridTextColumn Header="Monto" Binding="{Binding Monto, StringFormat='${0:N2}'}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="VerticalAlignment" Value="Center"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
